# 浏览器办公辅助插件需求文档（PRD）

## 一、文档信息



| 项目   | 内容                                              |
| ---- | ----------------------------------------------- |
| 文档名称 | 浏览器办公辅助插件 PRD 文档                                |
| 插件名称 | 办公助手（暂定名）                                       |
| 核心目标 | 解决办公场景中 JSON 格式化、时间戳转换、健康提醒三大高频痛点，提升办公效率与健康管理意识 |
| 适用对象 | 日常使用浏览器进行办公的个人用户（含职场职员、开发人员、数据处理人员等）            |
| 文档版本 | V1.0                                            |
| 编写日期 | 2026 年 03 月 12 日                                |
| 状态   | 初稿                                              |

## 二、需求背景

日常办公过程中，用户频繁面临三类核心问题：一是接口调试、数据核对时，JSON 字符串格式混乱难以阅读，手动格式化效率极低；二是跨系统对接、日志分析时，时间戳与标准时间的手动转换易出错、耗时长；三是久坐办公易忘记补水，影响身体健康且分散工作注意力。为解决上述痛点，特开发一款轻量级浏览器办公辅助插件，集成三大核心功能，贴合个人办公习惯，无需复杂操作即可提升办公效率与健康管理水平。

## 三、总体功能规划

插件整体分为三大核心模块，分别为**JSON 格式化模块**、**时间戳转换模块**、**定时喝水提醒模块**，采用 **Vue3 + TypeScript + Vite + Pinia** 技术栈开发，基于浏览器原生 API 实现，支持 Chrome、Firefox 等主流浏览器，数据本地存储，无需后端支持，操作流程简洁，无冗余功能干扰。

#### 技术栈说明

| 技术                    | 说明                                                                |
| ----------------------- | ------------------------------------------------------------------- |
| Vue3                    | 采用 Composition API 进行组件化开发，提升代码可维护性与复用性       |
| TypeScript              | 全量使用 TypeScript 进行类型约束，增强代码健壮性与开发体验          |
| Vite                    | 作为构建工具，提供快速的开发热更新与高效的生产构建                  |
| Pinia                   | 作为状态管理方案，管理提醒配置、转换记录等全局状态                  |
| CRXJS Vite Plugin       | Chrome Extension 开发插件，无缝衔接 Vite 构建流程，支持 HMR 热更新 |
| UnoCSS                  | 原子化 CSS 引擎，轻量高效，适合插件场景下的快速样式开发             |
| Chrome Extension MV3    | 采用 Manifest V3 规范，符合 Chrome 最新扩展标准                     |

#### 扩展架构说明

插件基于 Chrome Extension Manifest V3 架构，包含以下运行上下文：

| 模块             | 运行上下文       | 职责说明                                                                                   |
| ---------------- | ---------------- | ------------------------------------------------------------------------------------------ |
| Popup            | 弹出面板         | 三大功能模块的主界面，由 Vue3 渲染，承载 JSON 格式化、时间戳转换的交互操作及喝水提醒状态展示 |
| Background       | Service Worker   | 处理定时提醒逻辑，使用 `chrome.alarms` API 实现定时任务（而非 `setInterval`，适配 MV3 Service Worker 生命周期限制），触发桌面通知 |
| Storage          | chrome.storage   | 使用 `chrome.storage.local` 进行数据持久化，Pinia 通过封装的 storage adapter 与之对接       |

> **技术约束说明**：Manifest V3 的 Service Worker 空闲约 30 秒后会被浏览器回收，因此定时喝水提醒必须使用 `chrome.alarms` API 注册定时任务，而非依赖 `setInterval`/`setTimeout`。`chrome.alarms` 最小间隔为 1 分钟，满足本插件提醒精度要求。

#### 插件面板规格

| 属性   | 规格              |
| ------ | ----------------- |
| 宽度   | 400px（固定）     |
| 最小高度 | 500px           |
| 最大高度 | 600px（浏览器限制） |
| 布局   | 顶部 Tab 切换三大模块 |

## 四、详细功能需求

### （一）JSON 格式化模块

#### 1. 核心目标

实现 JSON 字符串的格式化、压缩、语法校验，提升 JSON 数据可读性与可用性，适配办公中接口调试、文档编写、数据核对等场景。

#### 2. 功能详情

##### （1）基础操作功能



* 支持用户手动输入或粘贴 JSON 字符串至插件面板输入框，支持 Ctrl+A 全选、Ctrl+Z 撤销、Ctrl+V 粘贴等常用快捷键操作。

* 提供「格式化」按钮，点击后自动对输入的 JSON 字符串进行标准化处理，实现自动换行、缩进对齐，同时支持语法高亮（key 为蓝色、字符串为绿色、数字为橙色、布尔值为紫色），提升阅读清晰度。

* 提供「压缩」按钮，点击后自动去除 JSON 字符串中所有空格、换行符，生成紧凑格式的 JSON 字符串，便于复制到接口参数、办公文档中。

* 内置 JSON 语法校验逻辑，输入非标准 JSON 字符串时，自动检测语法错误，精准定位错误位置（如第 X 行第 X 列），并给出明确错误提示（如「缺少结束引号」「逗号位置错误」），辅助用户快速修正。

##### （2）交互优化功能



* 支持格式化后 JSON 节点的折叠 / 展开操作，用户可点击节点前的「±」图标，隐藏或展开子节点，方便快速查看多层嵌套 JSON 结构。

* 格式化 / 压缩后的 JSON 内容，提供「一键复制」按钮，点击后将内容复制至剪贴板，复制成功后弹出轻量提示（如「复制成功」），提升操作效率。

* 支持自定义缩进格式，提供 2 空格、4 空格、Tab 三种缩进选项，用户可根据自身编码规范或文档要求选择，适配不同办公场景。

##### （3）可选拓展功能（V1.1 迭代规划）



* 支持自动修复非标准 JSON（如 key 无双引号、末尾多余逗号），修复后再进行格式化处理，降低用户手动修正成本。

#### 3. 典型使用场景



* 场景 1：接口调试时，复制后端返回的杂乱 JSON 字符串，粘贴至插件输入框，点击「格式化」，快速定位核心字段，辅助问题排查。

* 场景 2：编写办公文档时，将压缩后的 JSON 字符串通过「格式化」功能处理，提升文档可读性，便于团队协作查看。

* 场景 3：核对接口参数时，通过语法校验功能，快速发现 JSON 格式错误，避免因格式问题导致接口调用失败。

### （二）时间戳转换模块

#### 1. 核心目标

实现时间戳（秒 / 毫秒）与标准时间的双向自动转换，适配多格式时间需求，解决办公中时间数据转换繁琐、易出错的问题。

#### 2. 功能详情

##### （1）双向转换功能



* 支持「时间戳→标准时间」转换：用户输入 10 位（秒级）或 13 位（毫秒级）时间戳，插件自动识别输入类型，转换为「YYYY-MM-DD HH:MM:SS」标准格式，同时显示东八区时区信息。

* 支持「标准时间→时间戳」转换：用户通过输入框或时间选择器选择「YYYY-MM-DD HH:MM:SS」格式时间，插件自动生成对应的 10 位（秒级）和 13 位（毫秒级）两个时间戳，满足不同系统的时间格式要求。

* 自动识别输入内容类型，无需用户手动选择转换方向，实现智能转换，降低操作门槛。

##### （2）交互优化功能



* 提供「当前时间」快捷按钮，点击可快速获取当前系统时间，或生成当前时间对应的 10 位 / 13 位时间戳，提升操作效率。

* 转换结果区域提供「一键复制」按钮，支持复制标准时间或时间戳，复制成功后弹出提示，方便用户直接使用。

* 支持自定义时间格式，提供「YYYY 年 MM 月 DD 日 HH 时 MM 分 SS 秒」「MM/DD/YYYY」「YYYY-MM-DD」等多种格式选项，用户可根据需求切换，适配不同办公场景的时间展示规范。

* 本地保留最近 5 次转换记录，记录包含输入内容、转换结果、转换时间，用户可点击查看历史记录，快速回溯之前转换数据，记录支持一键清空。

##### （3）可选拓展功能（V1.1 迭代规划）



* 支持跨时区转换，提供 UTC 时间、纽约时间、东京时间等热门时区选项，满足跨国办公需求。

* 支持网页右键快捷转换：选中网页中的时间戳 / 时间文本，右键菜单中添加「转换为对应时间」选项，无需打开插件面板即可完成转换。

#### 3. 典型使用场景



* 场景 1：分析系统日志时，将日志中的 13 位毫秒时间戳转换为标准时间，定位问题发生的具体时间，辅助故障排查。

* 场景 2：编写接口文档时，将测试时间通过「标准时间→时间戳」转换，快速生成符合接口要求的时间戳参数，提升文档编写效率。

* 场景 3：跨系统数据对接时，统一不同系统的时间格式（部分系统用秒级戳，部分用毫秒级戳），快速完成格式转换，保障数据一致性。

### （三）定时喝水提醒模块

#### 1. 核心目标

通过轻量、不干扰的定时提醒方式，帮助办公用户养成定时喝水的习惯，同时支持自定义提醒规则，适配不同办公节奏。

#### 2. 功能详情

##### （1）提醒规则配置功能



* 支持自定义提醒间隔，默认设置为每 60 分钟提醒一次，用户可手动修改为 30 分钟、45 分钟、90 分钟等自定义时长，设置后即时生效。

* 提供「提醒时段设置」功能，支持选择办公时间段（如 9:00-18:00），插件仅在指定时段内触发提醒，非工作时段（如午休、下班时间）自动暂停提醒，避免干扰私人时间。

* 提供「暂停 / 恢复」功能，用户在开会、专注工作等场景下，可一键暂停提醒，并设置暂停时长（15 分钟、30 分钟、60 分钟），时长结束后自动恢复提醒，也可手动恢复。

##### （2）提醒方式与展示



* 采用「轻量优先」提醒策略：默认触发提醒时，仅通过浏览器右上角插件图标闪烁 + 桌面右下角轻量弹窗提示，弹窗内容为「该喝水啦！🥛 起身活动一下吧」，无声音干扰，避免影响办公环境。

* 可选加强提醒：支持用户开启「加强提醒」模式，提醒弹窗停留 10 秒不自动关闭，或需用户点击「已喝水」按钮后才能关闭；若未点击「已喝水」，5 分钟后将再次触发提醒，确保用户不会忽略。

* 默认关闭声音提醒，用户可手动开启声音提醒，选择轻量提示音，避免打扰同事。

##### （3）状态查看与快捷操作



* 插件面板首页实时展示「下次提醒时间」「今日已提醒次数」，让用户清晰掌握提醒状态。

* 提供快捷操作按钮，包括「立即提醒」（手动触发一次喝水提醒）、「跳过本次提醒」（临时跳过当前计划提醒，不影响后续定时提醒）、「暂停提醒」（快速暂停，无需设置时长）。

##### （4）数据持久化功能



* 本地存储用户自定义的提醒间隔、提醒时段、是否开启声音提醒等配置，浏览器重启后自动恢复配置，无需用户重新设置。

* 记录用户每日喝水提醒次数，生成简单的月度统计卡片，展示月度总提醒次数、按时喝水次数，辅助用户养成健康习惯。

#### 3. 典型使用场景



* 场景 1：专注编写代码、处理文档时，插件定时弹出轻量提醒，提醒用户喝水、活动，避免久坐忘记补水。

* 场景 2：进入会议前，点击「暂停提醒」，设置会议时长，避免会议中弹窗干扰，会议结束后自动恢复提醒。

* 场景 3：根据自身工作节奏，自定义提醒间隔为 45 分钟，适配高效办公节奏，保障身体健康的同时不影响工作进度。

## 五、非功能需求

### （一）性能需求



1. 插件启动响应时间≤1 秒，核心功能（JSON 格式化、时间戳转换）处理响应时间≤0.5 秒，确保操作流畅无卡顿。

2. 本地存储数据量控制在 10MB 以内，占用浏览器资源极低，不影响浏览器运行速度。

3. 提醒功能精准度高，误差≤1 分钟，满足日常办公提醒需求。

### （二）兼容性需求



1. V1.0 支持 Chrome 90+、Edge 90+（Chromium 内核）浏览器，确保核心功能正常运行。Firefox 支持列入 V1.1 迭代规划（需适配 WebExtension API 差异）。

2. 适配 Windows、macOS、Linux 等主流操作系统，界面展示与功能操作无差异。

### （三）易用性需求



1. 界面设计简洁直观，无复杂操作流程，普通用户无需学习即可快速上手三大核心功能。

2. 全流程提供操作提示与错误反馈，如复制成功提示、JSON 错误提示、转换结果提示等，降低用户操作困惑。

3. 支持常用快捷键操作，如 JSON 格式化的 Alt+Shift+J、时间戳转换的 Alt+Shift+T，提升操作效率（避免与浏览器内置快捷键冲突）。

### （四）安全性需求



1. 所有数据仅本地存储，不收集、不上传用户任何个人信息、办公数据，保障用户隐私安全。

2. 插件权限申请最小化，仅申请必要的浏览器原生 API 权限，避免权限滥用。V1.0 所需权限清单如下：

| 权限           | 用途说明                                       |
| -------------- | ---------------------------------------------- |
| `storage`      | 本地持久化存储用户配置、转换记录、喝水统计数据 |
| `alarms`       | 注册定时喝水提醒任务                           |
| `notifications`| 触发桌面通知弹窗提醒用户喝水                   |

## 六、开发与迭代规划

### （一）开发阶段（V1.0）



1. 完成插件基础框架搭建，实现三大核心模块的基础功能（JSON 格式化 / 压缩 / 校验、时间戳双向转换、定时喝水提醒）。

2. 完成基础交互与 UI 设计，确保界面简洁、操作流畅，适配主流浏览器与操作系统。

3. 完成本地存储逻辑开发，实现配置数据、转换记录的持久化存储。

### （二）迭代阶段（V1.1，可选）



1. 上线 JSON 模块的非标准 JSON 自动修复功能、时间戳模块的跨时区转换与右键快捷转换功能。

2. 优化提醒弹窗样式，添加自定义提醒文案功能，提升用户个性化体验。

3. 新增健康数据统计图表，直观展示用户喝水习惯养成情况。

## 七、验收标准



1. 功能验收：三大核心模块所有基础功能均正常实现，操作流程顺畅，无功能缺失、异常报错问题。

2. 性能验收：插件启动响应、功能处理响应符合性能要求，无卡顿、延迟过高现象，资源占用合理。

3. 兼容性验收：在指定浏览器与操作系统版本中，界面展示与功能操作正常，无适配问题。

4. 易用性验收：普通用户无需培训即可完成所有操作，提示清晰，反馈及时。

5. 安全性验收：无隐私数据收集、上传行为，权限申请符合最小化原则，无安全风险。

> （注：文档部分内容可能由 AI 生成）